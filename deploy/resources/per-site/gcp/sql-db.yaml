apiVersion: sql.cnrm.cloud.google.com/v1beta1
kind: SQLDatabase
metadata:
  annotations:
    cnrm.cloud.google.com/deletion-policy: abandon
  labels:
    label-one: "value-one"
  name: [WP_DB_NAME]
  namespace: [SITE_NS]
spec:
  charset: utf8
  instanceRef:
    name: wp-db
    namespace: default
